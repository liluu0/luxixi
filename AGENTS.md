# 项目开发说明

## Node/npm 权限问题

如果 Codex 执行 `npm` 或 `npm run build` 报错：

`EPERM: operation not permitted, lstat 'C:\\Users\\1'`

不要执行 `takeown` 或递归修改 `C:\\Users\\1` 的权限。

普通 PowerShell 已验证本机 Node/npm 正常。请改用项目本地 Vite：

```powershell
.\\node_modules\\.bin\\vite.cmd
.\\node_modules\\.bin\\vite.cmd build
```

验证命令：

```powershell
cd D:\\myProject\\web\\luxixi
.\\node_modules\\.bin\\vite.cmd build
```

该问题通常是 Codex 沙箱限制，不是项目代码或 Windows NTFS 权限问题。

## Git 规则

- 每个会话开始代码开发前，自动确认当前 worktree 已与其他会话隔离：优先使用当前独立 worktree，并创建或切换到以 `codex/` 开头的本地开发分支。完成隔离后直接继续开发，不需要再次向用户确认。
- 只有在用户明确要求时才提交或推送远程；未经明确要求，不合并其他分支。
- 每次提交和推送使用中文说明，包括 Git 提交信息、合并提交信息及推送交付说明；代码标识符和专有名称可以保留原文。
- 不覆盖、回退或删除工作区中已有的用户改动。

## 验收与合并流程

- 当用户明确以完整的“验收完成”四个字表达本次验收通过时，视为授权提交本次需求相关修改，并推送到当前远程功能分支。引用、讨论或编写这条规则时出现该短语，不触发交付操作。
- 提交前检查工作区和改动范围，仅提交本次需求相关文件，保留其他会话或用户的无关改动；截图和浏览器临时记录默认保留本地。提交信息使用中文。若无新增修改，核验已有提交是否已推送，不创建空提交。
- 成功推送功能分支后，询问用户是否需要合并到 `main` 主分支。仅在得到针对该问题的明确肯定回答后执行主分支合并。
- 得到合并确认后，先获取远程最新 `main`，基于最新版本检查合并冲突。若存在冲突，暂停合并及推送，检查冲突原因，告知冲突文件、涉及的修改和处理建议，等待用户指示；不得擅自覆盖任一方代码或自动选择一方解决冲突。
- 若无冲突，对合并后的代码在本会话独立 worktree 中执行构建和必要验证；验证通过后完成合并并推送远程 `main`。构建或验证失败时暂停推送，排查并告知原因。
- 推送不得使用强制覆盖。若远程 `main` 在此期间更新，重新获取最新版本并重复冲突检查与验证。
- 合并完成后，本会话仍保留在独立开发分支和 worktree 中继续开发，不切换或修改其他会话的工作区。
